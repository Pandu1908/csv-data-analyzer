# csv-data-analyzer
A simple Python CSV data analyzer that reads a CSV file and displays the total number of records, column names, and sample values from each column. It uses Python's csv module and handles missing files with an error message. This beginner-friendly project demonstrates file handling, dictionaries, lists, loops, and basic data analysis.
